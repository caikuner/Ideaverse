---
tags: [algo, handcode]
up:
related:
rank: "99"
companies:
created: 2025-06-18
modified: 2025-06-20
---

---

promise.limit
retry https://mp.weixin.qq.com/s/RcObOBl64Sj_4BAFCMu1tA?scene=1i
异步任务调度

lru

settime不准

数组、树
双向绑定
三级联动
指数运算

---

京东

- [x] 3.无重复字符的最长子串 （左指针移到上一个重复下标，但注意不要回退，取 min）
- [x] 146.LRU 缓存 （map 写法；双向链表）
- [x] 165.比较版本号 （split, parseint 比较每一位）
- [x] 34.在排序数组中查找元素的第一个和最后一个位置 （开区间二分查找下界：第一个 target 的坐标）
- [x] 704.二分查找 （背一个开区间写法） left + 1 < right; nums[mid] >= nums[right]
- [ ] 912.数组排序，要求O(nlog(n)) -> 快速排序 / 归并 / 堆
- [x] 150.逆波兰表达式求值 (元素入栈，遇到运算符就弹出两个计算，计算完结果也入栈)

---

美团

- [x] 415.大数相加 （先补前导 0 或者反转）
- [x] 15.三数之和 （排序后，左右双指针做两数之和；注意跳过重复值）
- [x] 1143.最长公共子序列(不连续) (动态规划, dp[i+1][j+1] = dp[i][j]+1 or Max(dp[i][j+1], dp[i+1][j]))
- [x] 最长公共子串(连续) （遍历，以 i,j 为起点滑动窗口）
- [x] 14.字符串数组最长公共前缀 => 变：最长公共后缀
- [x] 232.两个栈实现队列 （[]模拟栈，只允许push、pop；用两个栈：入栈、出栈。取值之前，入栈全部推到出栈）
- [x] 5.最长回文子串 （循环，以当前点找奇&偶回文。滑动窗口从中心向两边扩散）
- [x] 88.合并有序数组 （难点是要在 arr1 原地；从尾向前比较，这样比完能放到结尾的 0 占位位置）
- [x] 21.合并有序链表 （cur.next = nextNode； cur = cur.next）

- [x] 203.移除链表元素
- [x] 141.环形链表 是否有环 (slow = head, fast = head.next; slow = slow.next, fast = fast.next.next)
- [x] 142.环形链表 环的位置 (再加一个头指针，和 slow 一起走，直到相遇)
- [ ] 重排链表
- [x] 206.反转链表 (cur.next=prev; prev=cur; cur = next。最终 prev 到达 tail)
- [x] 92.反转链表（k 个节点） (找到 left.prev, right.next)

- [x] 102.二叉树层序遍历 (队列保存节点，上层节点从头出队，下层从尾进队)
- [x] 94.二叉树的中序遍历 (⚠️特殊；遍历 左中右。迭代:一直往左走，走到头弹出节点保存左，而后往上保存中，而后转向右。递归不需要变顺序)
- [x] 144.二叉树的前序遍历 (⭐️遍历：中左右。迭代入栈：(中)右左。递归不需要变顺序)
- [x] 145.二叉树的后序遍历 （遍历：左右中。迭代 反着变中右左，最后 reverse。递归不需要变顺序）
- [ ] 987.二叉树的垂序遍历 hard

---

快手

- [x] 165.比较版本号
- [x] 补充题4. 手撕快速排序
- [x] 146.LRU缓存机制
- [ ] 25.K 个一组翻转链表 （先找出节点个数；`p0=dummy, for (; n >= k; n -= k) -> for i<k；` 迭代一组后，重新指 p0: nxt=p0.next, p0.next.next=cur, p0.next=pre, p0=nxt）
- [x] 226.翻转二叉树 (栈，左右 push入栈，右左 pop 出栈；节点左右互换)
- [x] 104.二叉树的最大深度 (层序遍历，每层+1)
- [x] 662.二叉树的最大宽度 (层序遍历，每个节点记录上 index,2index,2index+1，每层取 尾首 index差值)
- [x] 20.有效的括号 (栈，左括号时入栈，右括号时检查栈顶的左是否匹配。匹配出掉这一对，否则 false。最终检查栈是否被匹配清空了)

- [x] 496.下一个更大的数 （单调栈；map 保存 nums1[v,i]; nums2单调栈 往前遍历找出x，res[map.get(x)] = 栈顶）
- [x] 503.下一个更大的数，单个数组循环 （单调栈；2n-1 往前遍历，x 取 i%n; res[i]= 栈顶）
- [x] 402.移掉 k 位数字 (贪心+单调栈； stk 留下更小的值，最多k 个)

- [x] 384.打乱数组 (Math.floor(Math.random() \* (left + right + 1)))

---

滴滴

- [x] 46.全排列 (回溯；固定每一位元素i，用used[]保存是否使用过；这一位分用、不用，不用的注意恢复现场=false；截止：i===n)
- [x] 47.全排列不重复 (回溯；相比 46，先排序，在固定元素时候保证和上一个使用过的不等即可)
- [x] 31.下一个排列 (从右往前找出第一个 x，使得其右边都是比它大的；从右边找一个刚好比 x 大的交换；右边全部 reverse)
- [x] 77.组合 （选：没选满都可以；不选：剩余待选元素>还需选择的元素）
- [x] 22.括号生成 (dfs(i, open), 填左括号：不超半数，填右括号：左>右)

选或不选
0-1 背包：494
完全背包： 322

- [x] 494.目标和 （回溯, +/-）
- [x] 零钱兑换 322，518 （回溯, 选或不选）

---

字节

- [x] 283.移动零 (左指针定位待放置元素位置，右指针扩散找非 0去放置，最后补 0)
- [x] 236.二叉树的最近公共祖先 (dfs left, right)
- [x] 316.去除重复字符，使字典序变小 （char < stk[stk.length - 1] && map.get(stk[stk.length - 1]) > i）
- [x] 344.反转字符串 （双指针）
- [ ] 146.LRU
- [x] 12.整数转罗马数字 (枚举；while(num>=val[i]), res += sys[i], num -= val[i])
- [x] 21.合并有序链表
- [x] 11.盛最多水的容器 （min(height[i], height[j]) \* (j-i)）
- [x] 42.接雨水 (while(left < right), leftMax, rightMax)
- [x] 32.最长有效括号 (stk[-1], res <- i-dtk顶)
- [x] 36进制加法 (和字符串加法一样，只是要用 parseInt(x, 36) 和 toString(36))
- [x] 572.B是否 A 的子树
- [x] 338.比特位 1 的个数（res[i] = res[i >> 1] + (i & 1)）
- [x] 538.二叉搜索树转累加树 （右-根-左 遍历，就可以保证之前的都大于当前值；用变量累计保存住即可）
- [x] 93.复原 IP 地址 (回溯； 不合法：剩余字符不足；不符合 ip：多个字符且有前导 0、大于 255)
- [x] 300.最长递增子序列 （二分查找，不断找更大的放进 res）

- [x] 43.字符串相乘 (for m ,n )
- [x] 394.字符串解码 （stk，遇见`[`就往前检查找次数，遇见`]`就往前检查找需要重复的chars）
- [x] 7.整数反转 (m%=10; res=res\*10 + m; x/=10; ~~)
- [x] 200.岛屿数量
- [x] 121.买卖股票的最佳时机 (minPrice; prices[i] - minPrice)
- [x] 54.螺旋矩阵 (top<=bottom; left<=right)
- [x] 79.单词搜索（矩阵中的路径）
- [x] 48.矩阵旋转 （对角线翻折；每行翻转）

---

- [ ] 239.滑动窗口最大值

- [ ] 889.根据前序和 中/后 序遍历构造二叉树
- [ ] 有序数组转二叉搜索树
- [ ] 验证二叉搜索树
- [ ] 课程表
- [ ] 946.验证栈序列
- [ ] 133.克隆图

---

Leetcode Hot 100
